import 'package:flutter/material.dart';

class ViewDocsPage extends StatelessWidget {
  const ViewDocsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("View Documents Page"),
    );
  }
}
